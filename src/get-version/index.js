import { commands } from 'src/get-version/commands'
import { controller } from 'src/get-version/controller'
import { repositories } from 'src/get-version/repositories'

export const getVersion = {
  controller,
  commands,
  repositories,
}
