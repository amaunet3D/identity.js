import { core } from 'src/domain/core'
import { getHealth } from 'src/domain/get-health'
import { getVersion } from 'src/domain/get-version'

export const domain = {
  core,
  getHealth,
  getVersion,
}
